import { Typography, AppBar } from "@mui/material";

const Footer = () => (
  <AppBar position="static" sx={{ mt: "auto" }}>
    <Typography color="black">Rick and Morty</Typography>
  </AppBar>
);

export default Footer;
